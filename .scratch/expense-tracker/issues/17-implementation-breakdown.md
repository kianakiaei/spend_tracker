# 17 — شکستن پیاده‌سازی به تیکت‌های اجرایی

Type: task
Status: open
Blocked by: —

## Question

با بسته‌شدن آخرین تیکت تصمیم (۱۵)، همهٔ تصمیم‌های نقشه تثبیت‌اند و طبق تصمیم کاربر این تلاش اجرا را هم خودش حمل می‌کند. پیاده‌سازی به تیکت‌های اجرایی بشکند و روی همین tracker منتشر شود (create-then-wire؛ هر تیکت = یک سشن ۱۰۰K با مرز و خروجی روشن):

- ورودی‌ها: Decisions so far نقشه (تیکت‌های ۰۱–۱۵)، `CONTEXT.md`، جهت‌گیری تأییدشدهٔ UI و پروتوتایپ v2 (تیکت ۰۷)، استراتژی تست (تیکت ۱۱) — پلن تست دقیق هر ماژول (مهٔ قبلی نقشه) به هر تیکت اجرایی می‌چسبد؛ فایل `src/lib/categorization/seed-lexicon.ts` (محتوای نهایی از ۱۳) در همین شکستن جای خود را پیدا می‌کند.
- ترتیب پیشنهادی: شروع با smoke-test اسکفولد (نصب Next.js 16 + Drizzle/Turso لوکال + better-auth ثبت‌نام/ورود — عدم‌قطعیت ثبت‌شدهٔ تیکت ۱۲) و بعد شاخه‌های دامنه: schema/migration (با اصلاح‌های ۱۴/۱۵)، ماژول جلالی، موتور دسته‌بندی، سرویس‌ها (با ensure و preview)، API v1، UI از پروتوتایپ، E2E.
- موازی و مستقل: [۱۶ — ثبت‌نام Turso/Resend و سکرت‌های prod] (HITL، با اکانت و مرورگر کاربر) — خروجی‌اش فقط envهای prod را تغذیه می‌کند و مسیرش نباید شکستن را نگه دارد.
- خروجی: تیکت‌های اجرایی شماره‌خورده با وابستگی‌های `Blocked by`، آمادهٔ کار با `/implement`.

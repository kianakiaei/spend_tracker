# 27 — جریان ثبت/ویرایش خرج: شیت پایینی + پیشنهاد زنده

Type: task
Status: resolved
Blocked by: 25, 26
Assignee: IDEHAL (agent session, 2026-09-07)

## Question

فرم ثبت و شیت ویرایش، طبق ترکیب تأییدشدهٔ ۰۷ و رفتار «همیشه پیشنهاد» ۰۶. **الزام: frontend-design**:

- **شیت ثبت پایینی** (دکمهٔ + اکسنت یشمی): عنوان؛ مبلغ با **نمایش زندهٔ fa-IR**؛ گام‌روز جلالی با `react-multi-date-picker` (`calendar={persian}` + `locale={persian_fa}`، state میلادی — تحقیق ۰۴)؛ **ماه هدف صریح**: «ثبت در مرداد ۱۴۰۵» (چون فرم می‌تواند از داشبورد ماه دیگر باز شود)؛ پیش‌فرض تاریخ: در ماه جاری = امروز؛ در ماه دیگر = **«بدون تاریخ»** (خرجِ بی‌تاریخ عضو ماه فرم — تصمیم ۱۵)؛ تاریخ‌دار همیشه به ماه خودش می‌رود حتی اگر با ماه فرم فرق کند.
- **چیپ دسته با بج «پیشنهاد»:** پیشنهاد زندهٔ سمت کلاینت با سرویس مشترک ۲۲ (واژه‌نامه + learnedKeys + دسته‌ها موقع لود در حافظه؛ پاسخ در میکروثانیه) — با تایپ عنوان به‌روزرسانی با **debounce ~۱۵۰ms**؛ تا وقتی کاربر دست به دسته نزده؛ **بعد از انتخاب دستی، موتور تا پایان همان فرم ساکت** و بج می‌افتد؛ fallback = پربسامدترین (خروجی classify سرویس)؛ بی‌تاریخچه = خوراکی. بی‌اثر بودن فرم هنگام تایپ (هیچ POSTی).
- **شیت ویرایش** (کلیک روی سطر خرج/از دریل‌داون): تغییر آزاد عنوان/مبلغ/تاریخ/دسته؛ دادن تاریخ به بی‌تاریخ → انتقال به ماه همان تاریخ (تصمیم ۱۵)؛ حذف با تأیید.
- ثبت/ویرایش/حذف از **fetch wrapper تایپ‌شدهٔ ۲۵** به `/api/v1/expenses` — نه Server Action مستقیم (قرارداد تیکت ۱۲: منطقِ فقط-اکشن در سرویس است).
- دست‌زدگی/خطای شبکه: پیام عمومی، حفظ دادهٔ فرم.

## Test plan

- component (jsdom + RTL): debounce پیشنهاد و سکوت بعد از تغییر دستی، افتادن بج، نمایش زندهٔ مبلغ fa-IR، چیپ «بدون تاریخ» و پیش‌فرض تاریخ per-ماه، نمایش ماه هدف؛ خوشی‌ها با E2E در ۳۰.

## Comments

- 2026-09-07 — **رزول شد: شیت ثبت/ویرایش + پیشنهاد زندهٔ کلاینت سبز — ۲۸۷ پاس (۲۵ فایل)، lint/tsc/build تمیز** — با مهارت frontend-design، از آناتومی تأییدشدهٔ نسخهٔ ۲ (FAB یشمی ثابتِ گوشه، scrim، پنل بالا‌آمده با فیلدهای خط‌کشی‌شده).
  - **پیشنهاد زنده (تصمیم ۰۶):** موتور خالصِ کلاینت `createSuggestionEngine` روی موتور ۲۱ — واژه‌نامه + learnedKeys + دسته‌ها موقع رندر RSC به‌صورت props به جزیرهٔ کلاینت می‌آیند (خواندن‌های سرویس: `listLearnedKeys` + `getFallbackCategory` استخراج‌شده از classify-service)؛ پاسخ در میکروثانیه، **هیچ POST هنگام تایپ**؛ پلهٔ null موتور = پاسخ fallback از پیش‌محاسبهٔ سرور («همیشه پیشنهاد» هرگز خالی نمی‌ماند)؛ debounce ۱۵۰ms؛ **بعد از انتخاب دستی موتور تا پایان همان فرم ساکت و بج می‌افتد**؛ شیت ویرایش از اول manual است (دستهٔ سطر، واقعیت است نه پیشنهاد — تفسیر ثبت‌شده). بعد از هر ثبت/ویرایش `router.refresh()` — یادگیری سمت سرور رخ داده و شمارنده‌های تازه با رندر بعدی به کلاینت می‌آیند.
  - **فرم:** عنوان؛ مبلغ با پارس نرم (`parseAmountInput` — ارقام فارسی/عربی/لاتین + جداکننده‌ها) و **نمایش زندهٔ fa-IR**؛ `react-multi-date-picker` با persian/persian_fa و state میلادی + چیپ «بدون تاریخ»؛ پیش‌فرض per-ماه (جاری = امروزِ تهران‌آگاه `currentTehranISODate`، غیر = بی‌تاریخ)؛ **ماه هدف صریح** که ماه مؤثر را صادقانه دنبال می‌کند (`effectiveMonthKey`: تاریخ‌دار → ماه خودِ تاریخ، بی‌تاریخِ ثبت → ماه فرم، بی‌تاریخِ ویرایش → ماه فعلیِ خرج)؛ در ویرایش «ذخیره در …» به‌جای «ثبت در …». حذف با تأیید درجا (danger از پالت ۰۷). ثبت/ویرایش/حذف فقط از fetch wrapper تایپ‌شدهٔ ۲۵؛ خطا = پیام عمومی + حفظ کامل دادهٔ فرم.
  - **داشبورد:** page حالا ۶ خواندن موازی؛ جزیرهٔ کلاینت `ExpenseSheetProvider` دور داشبورد، `Ledger` کلاینت شد (سطر خرج → شیت ویرایش؛ سطر پیش‌بینی لینک به /templates با ۲۸). افزودنی ماژول ۲۰: `currentTehranISODate`.
  - **خروجی دیداری (پنج حالت — اسکریپت [screenshots/capture-sheet.mjs](../screenshots/capture-sheet.mjs)):** [داشبورد+FAB](../screenshots/sheet-0-dashboard-fab.png)، [شیت ثبت تازه (fallback+بج)](../screenshots/sheet-1-create-fresh.png)، [بعد از تایپ (پیشنهاد حمل‌ونقل + مبلغ زنده)](../screenshots/sheet-2-create-suggestion.png)، [شیت ویرایش](../screenshots/sheet-3-edit.png)، [تأیید حذف](../screenshots/sheet-4-delete-confirm.png) — منتظر تأیید کاربر.
  - **تست:** ۱۱ کامپوننت (debounce/سکوتِ بعد از دستی/افتادن بج، مبلغ fa-IR، پیش‌فرض per-ماه + امروز در پیکر، ماه هدف، ثبت/ویرایش/حذف از کلاینت تایپ‌شده، خطا با حفظ داده) + ۷ unit برای کمک‌تابع‌ها + ۴ موتور پیشنهاد. **از code-review اعمال شد:** Tag مشترک (یک قرص اکسنت برای از الگو/پیش‌بینی/پیشنهاد)، یک کلاس دکمهٔ سطر دفتر، `LearnedKeyRecord` به‌جای تایپ هم‌شکل inline، عدم پارس مجدد مبلغ در save. **نیمهٔ دریل‌داونِ «از دریل‌داون» به ۲۸ می‌رود** (صفحهٔ /categories/[id] هنوز ساخته نشده — تیکت ۲۶ همین را برنامه‌ریزی کرده بود).

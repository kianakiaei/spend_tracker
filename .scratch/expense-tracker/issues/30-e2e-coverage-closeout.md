# 30 — E2E پنج‌جریان + بستن آستانه‌های پوشش + CI کامل

Type: task
Status: resolved
Blocked by: 26, 27, 28, 29

## Question

بستن قید «تست درست و با پوشش» (اولویت صریح کاربر) طبق لایهٔ چهارم تحقیق ۱۱:

- **Playwright 1.63، پنج جریان:** ① ورود/ثبت‌نام (toggle + خطای عمومی) ② افزودن خرج (پیشنهاد زنده + تغییر دستی + مبلغ fa-IR) ③ ناوبری ماه (گذشته خالیِ ثبت‌شده، آینده با پیش‌بینی و بج) ④ الگوی تکرار (ساخت الگو → «خرج این ماه تولید شد» → ویرایش آزاد خرجِ تولیدشده) ⑤ انقضای سشن (بعد از ۴۰۱ → `/login`).
- **reuse سشن** با `storageState` از `POST /api/auth/sign-in/email` (یک بار در global-setup).
- **آستانه‌های per-glob نهایی و سبز** (تصمیم تأییدشدهٔ ۱۱/۱۲): `src/lib/{categorization,jalali,recurring}` ≥۹۰٪ خط / ≥۸۵٪ شاخه؛ `services` ≥۸۰/۷۵؛ UI بدون آستانه.
- CI کامل و سبز: lint + test + e2e + build در GitHub Actions بدون service container.
- جاروی باقی‌مانده‌ها: TODOها، حالت‌های خطای خالی، audit دسترسی‌ها (هر endpoint 401 بدون سشن) — گزارش کوتاه در کامنت تیکت.

## Test plan

- خود این تیکت E2E است؛ خروجی = پنج جریان سبز + گزارش پوشش per-glob در حد آستانه‌ها + CI سبز. عقب‌نشینی ثبت‌شده در صورت friction: vitest 5 → 4.x، TS 7 → 5.9.3.

## Comments

- 2026-09-09 — **پنج جریان سبز شد (۹/۹ با setup؛ ‏prod build‎ روی دیتابیس تازه) + ‏vitest‎ ‏۳۳۳ پاس + پوشش بالای آستانه‌ها**.
  - **سوئیت (`e2e/`):** ‏`global-setup.ts`‎ سطرها را پاک و مایگریت می‌کند (حذف فایل روی ویندوز ‎EBUSY‎ می‌داد)؛ ‏`auth.setup.ts`‎ یک کاربر را از UI ثبت‌نام و ‏storageState‎ را فریز می‌کند؛ پنج فلو: ① گیت لاگ‌اوت/تاگل/خطای عمومی/ثبت‌نام ② ثبت خرج (پیشنهاد زنده + تغییر دستی + مبلغ fa-IR) ③ ناوبری ماه (گذشته خالی، آینده با بج پیش‌بینی) ④ الگو → «خرج این ماه تولید شد» → ویرایش آزاد سطر تولیدشده ⑤ انقضای سشن (ریدایرکت ‎/login‎ + ‏401‎ تمیز با ‎storageState‎ خالی).
  - **باگ واقعی ‎P0‎ که فلوها گرفتند:** بدنه‌های ‎201‎ با اعتبارسنجی کلاینت تایپ‌سیف رد می‌شدند ولی سرور ذخیره می‌کرد (شیت همیشه «ذخیره نشد») — ‏`userId`‎ واقعی better-auth رشتهٔ opaque است نه ‎UUIDv7‎، و ‎POST/PATCH‎ خرج سطر بدون ‎`category`‎ برمی‌گرداند. فیکس: ‏`authUserIdSchema`‎ در پاسخ‌ها + بازخوانی سطر کامل در هندلرها. چهار تست رگرسیون contract (بدنه در برابر اسکیمای مشترک) اول قرمز، بعد سبز.
  - **پوشش نهایی:** ‏categorization‎ ‏۹۹٫۴/۹۵٫۲‎، ‏jalali‎ و ‏recurring‎ ‏۱۰۰/۱۰۰‎، ‏services‎ ‏۹۸٫۲/۹۲٫۴‎ (آستانه‌ها: ۹۰/۸۵ و ۸۰/۸۰). جارو: بدون ‎TODO‎ در ‎src‎؛ ‎401‎ همهٔ endpointها در contract + e2e؛ حالت‌های خالی/خطا در فلوها.
  - **انحراف‌های ثبت‌شده:** احراز مشترک از پروژهٔ ‎setup‎ + ثبت‌نام UI است (نه ‎POST‎ در global-setup — همان خاصیت یک‌بار-احراز، واقعی‌تر)؛ ‏`reuseExistingServer: false`‎ (سرور dev روی ‎:3000‎ دیتابیس اشتباه را تست می‌کرد — تصادم باید بلند fail شود). گام بعدی: ۳۱ (دیپلوی)؛ ۱۶ مستقل و فقط گاز ۳۱.

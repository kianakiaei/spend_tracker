# 21 — موتور دسته‌بندی + واژه‌نامهٔ سیستمی src/lib/categorization

Type: task
Status: resolved
Blocked by: 18
Assignee: IDEHAL (agent session, 2026-09-06)

## Question

موتور خالص TypeScript بدون وابستگی ML، عین تحقیق ۰۱ + محتوای تثبیت‌شدهٔ تیکت ۱۳:

- `src/lib/categorization/seed-lexicon.ts` **عین محتوای کامنت تیکت ۱۳** (۷۴ کلید کانونی: ۶۸ توکنی + ۶ عبارتی در `SYSTEM_CATEGORIES` + `SEED_LEXICON`) — فقط داده؛ ناوردای «هر کلید کانونی است» تست می‌شود.
- نرمال‌ساز فارسی (~۲۰ خط تحقیق ۰۱): unify ي/ی و ك/ک، NFKC، حذف اعراب/تنوین/کشیده/علائم RTL، یکسان‌سازی ارقام فارسی/عربی به لاتین، ZWNJ = فاصله، فشرده‌سازی فاصله‌ها.
- دو Map درون‌حافظه‌ای (عبارتی/توکنی): فلت‌کردن `tokens` → Map توکنی و `phrases` → Map عبارتی؛ **عبارت‌ها به توکن‌های سازنده تجزیه نمی‌شوند** (دلیل جدا بودن فیلدها — تیکت ۱۳).
- نردبان شش‌پله‌ای: learned phrase > learned token > lexicon phrase > lexicon token > prefix > fuzzy (Levenshtein Δ1، توکن ≥۴ حرف) > null — «عبارت قبل از توکن» بار معنایی دارد («اسنپ فود» باید از «اسنپ» جلو بزند).
- جست‌وجوی عبارت: کل عنوان کانونی + بگرام‌های مجاورِ توکن‌های معنادار («اسنپ فود شام» → بگرام «اسنپ فود»).
- confidence = purity × support شمارنده‌ها — فقط گزارشی در پاسخ، نه گِیت تصمیم (تصمیم «همیشه پیشنهاد» تیکت ۰۶).
- استخراج کلید برای یادگیری: عبارت + توکن‌های بارز بدون stopword («خرید نان»/«نان خریدم» هر دو «نان») + تابع خالص learn (count++) و decay (×۰٫۵ در تناقض) روی شمارنده‌ها — نوشتن SQL با تیکت ۲۲ است.
- موتور کاملاً خالص و تزریقی: ورودی = واژه‌نامهٔ سیستمی + learnedKeys کاربر + فهرست دسته‌های او (id/name/slug)؛ **حل slug → UUID بیرون موتور** انجام می‌شود؛ خروجی حدس = `{categoryId | null, source: 'learned'|'system', matchedKey, confidence}`.

## Test plan

- unit در project node — آستانهٔ این glob فعال شود: `src/lib/categorization` ≥۹۰٪ خط / ≥۸۵٪ شاخه.
- جدول §۸ تیکت ۱۳ **سطر به سطر** (نون، تاکسی اینترنتی، قطار شهری، نان سبوس‌دار با سه املای نیم‌فاصله، قبضِ برق، کارت به کارت → null، خرید نان، «خرید»/«بیمه» تنها → null، «2000»/«Coffee» → null، دندون/اوتوبوس fuzzy، اسنپ فود/اسنپ مارکت قبل از توکن اسنپ) + ناوردای کانونی بودن کلیدها + تست‌های learn/decay + پوشش نرمال‌ساز.

## Comments

- 2026-09-06 — **رزول شد: موتور خالص سبز با پوشش ۱۰۰٪ خط / ۹۳٪ شاخه** (آستانهٔ ۹۰/۸۵ فعال در vitest.config.mts)، lint/tsc/vitest 130 (چهار فایل unit جدید: seed-lexicon.test.ts + categorization.test.ts) همگی پاس. ساختار: `normalize.ts` (نرمال‌ساز ~۲۰ خط تحقیق ۰۱)، `keys.ts` (stopword ~۲۰تایی + `extractKeys` = کل عبارت کانونی + بگرام‌های باقی‌مانده + یونیگرام‌های بارز — درز یادگیری تیکت ۲۲)، `engine.ts` (موتور تزریقی + `learn`/`decay` خالص روی ردیف‌های `LearnedKeyRecord`)، `seed-lexicon.ts` (از تیکت ۱۹، محتوایش با تیکت ۱۳ وصل چک شد). نردبان شش‌پله‌ای دست‌نخورده؛ عبارت‌ها هرگز تجزیه نمیشوند؛ confidence فقط گزارشی. تصمیم‌های تفسیری ثبت‌شده:
  - **«دندون» §۸:** Levenshtein(دندون، دندانپزشک) = ۵ — با سیدِد فروزن هیچ شریکِ Δ1 ندارد؛ رفتار محافظه‌کارانهٔ سیدِد = null (purity اول). پلهٔ fuzzy روی «کلیدهای یادگرفته اول، بعد واژه‌نامه» پیاده شد و سطر §۸ با state یادگرفتهٔ «دندان» → درمان سبز است؛ «اوتوبوس» با سیدِد واقعی سبز.
  - **pلهٔ fuzzy = فقط Δ1/≥۴ حرف** طبق متن همین تیکت؛ باندٔ دوم تحقیق ۰۱ (≤۲ برای ≥۷ حرف) عمداً نیامد.
  - **confidence = جفتِ {purity, support}** — عین قرارداد endpoint تیکت ۱۲ (`confidence: { purity, support } | null`)؛ ضرب به‌عنوان عدد واحد جایی محاسبه نمیشود؛ «فقط گزارشی» رعایت شد (هیچ پله‌ای گِیت عددی ندارد). lexicon/prefix/fuzzy-سیستمی → confidence: null.
  - **«حل slug → UUID بیرون موتور» ↔ تیکت ۱۳ («موتور در زمان لود حل می‌کند»):** موتور هیچ SQL نمی‌خواند — فهرست دسته‌های کاربر (id/name/slug) تزریقی می‌آید و نگاشت slug→id در ساخت موتور از همان لیست ساخته میشود؛ خواندن ردیف‌ها بیرون است (تیکت ۲۲). اسلاگِ غایب → کلیدهایش ساکت.
  - **پلهٔ prefix** (شکل دقیقش در تیکت باز بود) سه تطبیق دارد: عنوان-کلید را دراز می‌کند («قسطی»→«قسط»)، کلید عنوانِ ناتمام را کامل می‌کند («نانوا»→«نانوایی»)، و کلید توکنِ پایانیِ معنادار را («خرید نانوا»→«نانوایی»)؛ گارد عنوان ≥۳ حرف تحقیق ۰۱ + دمِ ≥۲ حرف؛ بلندترین کلید، یادگرفته قبل از سیستمی.
  - **قطع تساوی شمارنده‌ها** با ترتیب دسته‌های کاربر (determinism؛ هم‌خوان با fallback «دستهٔ سیستمی اول» تیکت ۰۶).
  - ردیف‌های count ≤ 0 یا با دستهٔ حذف‌شده نه پیشنهاد میدهند نه purity را می‌شکنند (defensive؛ هرس واقعی SQL تیکت ۲۲).
  - گام بعدی: تیکت ۲۲ آزاد شد (سرویس‌های دسته/خرج + یادگیری هنگام ذخیره با همین درزها: `extractKeys` + `learn`/`decay`).

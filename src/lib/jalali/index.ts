// Re-export of the shared Jalali calendar module (expo-mobile ticket 01).
// @spend-tracker/shared is the single source of truth; this module stays
// so existing `@/lib/jalali` imports keep working with zero change.
export * from "@spend-tracker/shared/jalali";
